# What's new

(Write the next release's user-facing notes here before tagging. The release
workflow uses this file as the body and appends the checksum, VirusTotal scan
and build-provenance links automatically.)

## Install / update

1. Download `Club_Training_vX.Y.Z.zip` below and unzip anywhere. No Python or internet needed.
2. **Updating?** Copy your `badminton.db` (and `backups` folder) from the old app folder into the new one first.
3. Double-click `Club_Training.exe`.
